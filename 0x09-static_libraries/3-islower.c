#include "main.h"

/**
 *  * _islower - checks for lowercase character
 *   * @c: the character to check
 *    * Return: ! if lowercase, o0 otherwise
 *     */
int _islower(int c)

{
		return (c >= 'a' && c <= 'z');
}

